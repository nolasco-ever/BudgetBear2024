import {SafeAreaView, ScrollView, Text} from 'react-native';

export const HomeScreen = () => {
  return (
    <SafeAreaView>
      <ScrollView>
        <Text>HOME SCREEN</Text>
      </ScrollView>
    </SafeAreaView>
  );
};
